import React from 'react';
import styled from './auth.module.scss';

function Auth() {
  return (
    <div className={styled.centreH}>
      <div className={styled.centreV}>
        <div className={styled.auth} />
      </div>
    </div>
  );
}

export default Auth;
